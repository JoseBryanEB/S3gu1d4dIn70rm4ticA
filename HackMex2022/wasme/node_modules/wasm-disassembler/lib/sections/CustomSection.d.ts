import { Section, SectionIds } from './Section';
export declare class CustomSection extends Section {
    readonly sectionId: SectionIds;
    name: string;
    subpayLoad: Uint8Array;
    ReceivePayload(payload: {
        name: string;
        subpayLoad: Uint8Array;
    }): void;
}
