"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class CustomSectionDisassembler {
    constructor(binaryProvider) {
        this.binaryProvider = binaryProvider;
    }
    Disassemble(initialPointer) {
        let name;
        let pointer = initialPointer;
        [name, pointer] = this.binaryProvider.ReadName(pointer);
        const subPayload = this.binaryProvider.Slice(pointer, pointer + 1);
        return { name, subPayload };
    }
}
exports.CustomSectionDisassembler = CustomSectionDisassembler;
//# sourceMappingURL=CustomSectionDisassembler.js.map