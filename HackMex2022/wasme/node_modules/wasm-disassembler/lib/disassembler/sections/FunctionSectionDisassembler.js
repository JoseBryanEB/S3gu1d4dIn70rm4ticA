"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const WasmFunction_1 = require("../../functions/WasmFunction");
class FunctionSectionDisassembler {
    constructor(binaryProvider) {
        this.binaryProvider = binaryProvider;
    }
    Disassemble(initialPointer) {
        let pointer = initialPointer;
        let functions = [];
        const numberOfFunctionsLenght = this.binaryProvider.Getu32(pointer);
        pointer += numberOfFunctionsLenght.BytesUsed;
        while (functions.length < numberOfFunctionsLenght.Value) {
            const newFunc = new WasmFunction_1.WasmFunction();
            newFunc.Index = this.binaryProvider.GetRawByte(pointer);
            pointer++;
        }
        return functions;
    }
}
exports.FunctionSectionDisassembler = FunctionSectionDisassembler;
//# sourceMappingURL=FunctionSectionDisassembler.js.map