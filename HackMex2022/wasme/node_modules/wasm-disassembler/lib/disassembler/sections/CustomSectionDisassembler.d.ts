import { SectionDisassembler } from './SectionDisassembler';
import { WasmBinaryProvider } from '../../binaryProvider/WasmBinaryProvider';
export declare class CustomSectionDisassembler implements SectionDisassembler {
    private binaryProvider;
    constructor(binaryProvider: WasmBinaryProvider);
    Disassemble(initialPointer: number): {
        name: string;
        subPayload: Uint8Array;
    };
}
